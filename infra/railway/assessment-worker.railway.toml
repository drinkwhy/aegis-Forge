[build]
builder = "docker"
dockerfilePath = "services/assessment-worker/Dockerfile"

[deploy]
startCommand = "/app/assessment-worker"
healthcheckPath = "/health/ready"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
numReplicas = 1
