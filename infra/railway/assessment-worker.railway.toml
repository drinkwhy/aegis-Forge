[build]
builder = "DOCKERFILE"
dockerfilePath = "services/assessment-worker/Dockerfile"
buildContext = "services/assessment-worker"

[deploy]
healthcheckPath = "/health/ready"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
