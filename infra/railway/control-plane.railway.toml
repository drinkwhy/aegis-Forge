[build]
builder = "docker"
dockerfilePath = "services/control-plane/Dockerfile"

[deploy]
startCommand = "/app/control-plane"
healthcheckPath = "/health/ready"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
numReplicas = 1
