[project]
name = "aegisagent"
version = "0.1.0"
description = "Aegis Forge Python Agent SDK"
authors = [{name = "Aegis Forge"}]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
