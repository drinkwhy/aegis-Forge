{
  "name": "aegis-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Aegis Forge — Continuous AI Security Validation Platform",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "web": "turbo run dev --filter=web",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
