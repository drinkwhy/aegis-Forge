[build]
builder = "DOCKERFILE"
dockerfilePath = "services/analysis-engine/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
